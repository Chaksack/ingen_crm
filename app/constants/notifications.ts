import type { NotificationItems } from '~/types/notifications'

export const notificationItems: NotificationItems[] = [

  {
    id: '1',
    title: 'New User Registered',
    description: 'A new user has just registered on the platform.',
  },
]
